diff --git a/frameworks/intl/parameter_upgrade/src/signature_verifier.cpp b/frameworks/intl/parameter_upgrade/src/signature_verifier.cpp
index 0b7ae35..919575f 100644
--- a/frameworks/intl/parameter_upgrade/src/signature_verifier.cpp
+++ b/frameworks/intl/parameter_upgrade/src/signature_verifier.cpp
@@ -14,6 +14,7 @@
  */
 
 #include <climits>
+#include <cmath>
 #include <cstdlib>
 #include <cstring>
 #include <fcntl.h>
@@ -223,7 +224,7 @@ std::string SignatureVerifier::CalcFileSha256Digest(const std::string& path)
 
 void SignatureVerifier::CalcBase64(uint8_t* input, uint32_t inputLen, std::string& encodedStr)
 {
-    size_t base64Len = static_cast<size_t>(ceil(static_cast<long double>(inputLen) / BASE64_ENCODE_PACKET_LEN) *
+    size_t base64Len = static_cast<size_t>(ceil(static_cast<double>(inputLen) / BASE64_ENCODE_PACKET_LEN) *
         BASE64_ENCODE_LEN_OF_EACH_GROUP_DATA + 1);
     std::unique_ptr<unsigned char[]> base64Str = std::make_unique<unsigned char[]>(base64Len);
     int encodeLen = EVP_EncodeBlock(reinterpret_cast<uint8_t*>(base64Str.get()), input, inputLen);
