diff --git a/compile_standard_whitelist.json b/compile_standard_whitelist.json
index 008ad010..521d86ae 100644
--- a/compile_standard_whitelist.json
+++ b/compile_standard_whitelist.json
@@ -311,7 +311,9 @@
         "//third_party/flutter/build/skia:skia_shared",
         "//third_party/vk-gl-cts/framework/platform/ohos/rosen_context:rosen_context",
         "//third_party/vk-gl-cts/framework/platform:libdeqp_ohos_platform",
-        "//ide/tools/previewer:lite_previewer"
+        "//ide/tools/previewer:lite_previewer",
+        "//third_party/asmjit:libasmjit",
+        "//third_party/asmjit:libasmjit_frontend_static"
     ],
     "external_deps_added_self_part_module": [
         "//build/rust/tests/test_bin_crate:test_bin_crate",
diff --git a/config/BUILDCONFIG.gn b/config/BUILDCONFIG.gn
index 5930ced3..c87bc75f 100755
--- a/config/BUILDCONFIG.gn
+++ b/config/BUILDCONFIG.gn
@@ -385,8 +385,7 @@ is_wearable_product = "${target_platform}" == "wearable"
 
 is_intellitv_product = "${target_platform}" == "intellitv"
 
-if ((target_os == "ohos" && target_cpu == "x86_64") ||
-    device_company == "emulator") {
+if (device_company == "emulator") {
   is_emulator = true
 }
 
diff --git a/config/ohos/BUILD.gn b/config/ohos/BUILD.gn
index 26b10a17..e59de7c2 100755
--- a/config/ohos/BUILD.gn
+++ b/config/ohos/BUILD.gn
@@ -77,7 +77,7 @@ config("runtime_library") {
 
   # arm builds of libc++ starting in NDK r12 depend on unwind.
   if (current_cpu == "arm" || current_cpu == "arm64" ||
-      current_cpu == "riscv64") {
+      current_cpu == "riscv64" || current_cpu == "x86_64") {
     libs += [ "unwind" ]
   }
 
diff --git a/ohos/images/build_image.py b/ohos/images/build_image.py
index d0ebca52..c2f16962 100755
--- a/ohos/images/build_image.py
+++ b/ohos/images/build_image.py
@@ -40,7 +40,8 @@ def _prepare_root(system_path: str, target_cpu: str):
     os.makedirs(root_dir, exist_ok=True)
     _dir_list = [
         'config', 'dev', 'proc', 'sys', 'updater', 'system', 'vendor', 'data', 'chip_ckm',
-        'storage', 'mnt', 'tmp', 'sys_prod', 'chip_prod', 'module_update', 'eng_system', 'eng_chipset'
+        'storage', 'mnt', 'tmp', 'sys_prod', 'chip_prod', 'module_update', 'eng_system', 'eng_chipset',
+        'run', 'var', 'opt', 'usr'
     ]
     for _dir_name in _dir_list:
         os.makedirs(os.path.join(root_dir, _dir_name), exist_ok=True)
@@ -48,7 +49,7 @@ def _prepare_root(system_path: str, target_cpu: str):
     os.symlink('/system/bin/init', os.path.join(root_dir, 'init'))
     os.symlink('/system/etc', os.path.join(root_dir, 'etc'))
     os.symlink('/vendor', os.path.join(root_dir, 'chipset'))
-    if target_cpu == 'arm64' or target_cpu == 'riscv64':
+    if target_cpu == 'arm64' or target_cpu == 'riscv64' or target_cpu == 'x86_64':
         os.symlink('/system/lib64', os.path.join(root_dir, 'lib64'))
     os.symlink('/system/lib', os.path.join(root_dir, 'lib'))
 
diff --git a/subsystem_config.json b/subsystem_config.json
index 578ac460..cb2f4a8b 100644
--- a/subsystem_config.json
+++ b/subsystem_config.json
@@ -230,5 +230,9 @@
   "game": {
     "path": "domains/game",
     "name": "game"
+  },
+  "oniro_soc_products": {
+    "path": "device/soc/oniro",
+    "name": "oniro_soc_products"
   }
 }
diff --git a/toolchain/mini_debug_info.py b/toolchain/mini_debug_info.py
index 964ab1f1..9eafdd53 100755
--- a/toolchain/mini_debug_info.py
+++ b/toolchain/mini_debug_info.py
@@ -88,7 +88,7 @@ def create_mini_debug_info(binary_path, stripped_binary_path, root_path, clang_b
         " --only-keep-debug " + binary_path + " " + debug_path
     gen_mini_debug_cmd = llvm_objcopy_path + " -S --remove-section .gdb_index --remove-section .comment --keep-symbols=" + \
         keep_path + " " + debug_path + " " + mini_debug_path
-    compress_debuginfo = "xz " + mini_debug_path
+    compress_debuginfo = "xz -f " + mini_debug_path
     gen_stripped_binary = llvm_objcopy_path + " --add-section .gnu_debugdata=" + \
         mini_debug_path + ".xz " + stripped_binary_path
 
